# Procfile pour les services Celery sur Railway

# Service principal Django
web: python manage.py runserver 0.0.0.0:$PORT

# Service Celery Worker (pour exécuter les tâches)
celery-worker: celery -A event_management worker --loglevel=info --concurrency=2

# Service Celery Beat (pour programmer les tâches périodiques)
celery-beat: celery -A event_management beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler
